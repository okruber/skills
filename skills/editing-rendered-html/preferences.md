# Learned preferences

Recurring nudge patterns distilled from finalized pages. Consulted at authoring
time (step 1) so predictable adjustments happen before the first render.

Rules:
- An entry qualifies only after the same pattern appears in three or more finalized pages.
- Each entry is dated and concrete (direction, magnitude, context).
- One-off tweaks never enter this file. Keep it under 30 lines; drop entries that no longer hold.
